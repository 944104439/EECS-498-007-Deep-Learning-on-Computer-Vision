# Commonly used method in PyTorch 

```python
1. torch.cat([loop])
2. torch.chunk(tensor, nums)
3. "broadcast (1,nums1) + (nums2,1)"  
4. "frequent" vals,indices = torch.mode(tensor,dim)
```

